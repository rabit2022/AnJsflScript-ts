import {Width} from "../../Base/SimpleControls.types";

export interface CheckboxAttributes extends Width {
    // label?: string;
    checked?: boolean;
    tabindex?: number;
    accesskey?: string;
}

