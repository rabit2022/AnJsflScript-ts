src/
├── config/
│   └── paths.ts        # 文件路径配置
│
├── core/
│   ├── parser.ts       # 栈解析
│   ├── formatter.ts    # 输出格式化
│   ├── normalizer.ts   # 输入归一化
│
├── storage/
│   ├── fileStore.ts    # 文件读写
│   └── historyStore.ts # 错误历史管理
│
├── api/
│   └── stackTracer.ts  # 对外主入口
│
├── types/
│   └── index.ts
│
└── index.ts            # 导出
