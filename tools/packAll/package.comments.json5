{
  "name": "packAll",
  "version": "1.0.0",
  "description": "",
  "declaration": true,
  "declarationMap": true,
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    // 生成package.json
    "preinstall": "npm install json5 && npx tsx ./tools/package/sync-package.ts",



    // 流程1
    // build
    // babel
    // iife
    "build:01.webpack": "webpack --config webpack.config.js && npx babel out/packAll.js --out-file out/packAll.jsfl && npx tsx ./tools/iife/wrap-out-jsfl.ts",

    // 流程2
    // build
    // babel
    // iife
    "build:01.browserify":"npx browserify ./src/entry.ts -o ./out/packAll.js && npx babel out/packAll.js --out-file out/packAll.jsfl && npx tsx ./tools/iife/wrap-out-jsfl.ts",

    // 流程3
    // 复制packAll.js -> index.ts
    // tsc -> dist/index.js
    // babel翻译方法太多，现在直接作为ts，自动翻译，helpers减少很多
    // 但是可能不会按照预期执行，此时只能babel方案
    "build:02.tsc": "npx tsx tools/package/pack_ts.ts && tsc && npx tsx ./tools/copy/copy-js-to-jsfl.ts   && npx tsx ./tools/iife/wrap-dist-jsfl.ts",

    // 独立的翻译代码
    "build:12:babel": "npx babel out/packAll.js --out-file out/packAll.jsfl && npx tsx ./tools/iife/wrap-out-jsfl.ts",

    // 独立的ts文件打包
    "build:11.tsc": "tsc && npx tsx ./tools/iife/wrap-dist-jsfl.ts",
    "build:21.tsc": "tsc",

    "test": "jest",
    "build:test": "webpack --config webpack.test.js",
    "generate-test": "npx tsx tools/generate-test/generate-test.ts",
    "exclude_modules": "npx tsx tools/exclude_packages/exclude_require_config.ts"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    // babel
    "@babel/cli": "^7.28.6",
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.0",

    // jest
    "@types/jest": "30.0.0",
    "@types/node": "25.0.7",
    "jest": "30.2.0",
    "ts-jest": "29.4.6",

    // webpack
    "strip-json-comments": "3.1.0",
    "terser-webpack-plugin": "5.3.16",
    "webpack": "5.104.1",
    "webpack-cli": "6.0.1",

    //  ts
    "ts-loader": "9.5.4",
    "typescript": "5.9.3",
  }
}
