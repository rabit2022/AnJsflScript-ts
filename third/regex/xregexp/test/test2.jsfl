
require(["util"],function (util) {
    const json = {
        a: 20, b: "bbbb"
    }
    // window.console.inspect = util.inspect;


    // const result = util.inspect(json);
    // console.log(result);

    console.inspect(json);
});
